#include "main.h"

/**
 * a - computes the absolute of an integer.
 *
 * @int: the character
 * Return: Always 0 (Success)
 */

int _abs(int a)
{
	if (a < 0)
	{
		return (-t);
	}
	else if (a > 0)
	{
		return (a);
	}
	else
		return (0);
}
