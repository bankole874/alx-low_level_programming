#include "main.h"

/**
 * _abs - computes the absolute of an integer.
 *
 * @int: the character
 * Return: Always 0 (Success)
 */

int _abs(int)
{
	if (int < 0)
	{
		return (int * (-1));
	}
	else if (int > 0)
	{
		return (int);
	}
	else
		return (0);
}
